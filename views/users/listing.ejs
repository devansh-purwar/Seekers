<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Seekers</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/media/edit.ico">
    <link rel="stylesheet" href="/css/listing.css">
</head>

<body>
    <div style="min-height:calc(100vh - 70px);">
    <%- include('../partials/flash')%>
    <% if (currentUser) { %>
        <%- include('../partials/login_navbar') %>
            <% } else { %>
                <%- include('../partials/navbar') %>
                    <% } %>
                    <div class="container">
                        <h1 class="mt-3">My Listings</h1>

                        <div class="container mt-3">
                            <div class="row">
                                <% if (rooms.length) { %>
                                 
                                    <% for( let i = 0; i < rooms.length; i++ ) { %> 
                                
                                <div class="col-xl-3 mt-3">
                                    <div class="image">
                                        <h3><%= rooms[i].address %></h3>
                                        <div >
                                            <div class="d-flex justify-content-center mt-4">
                                        <img class="room img-fluid rounded" src="<%= rooms[i].images[0].url %>" style="height:248px ;width:250px;object-fit: cover;" alt="Room photo">
                                    </div>
                                    </div>
                                        <div class="d-flex justify-content-between mt-3">
                                        <a href="<%="/rooms/"+ rooms[i]._id%>">
                                        <button type="button" class="btn btn-danger">View</button></a>
                                        <form action="/rooms/<%=rooms[i]._id%>?_method=DELETE"
                                            method="POST">
                                    <button class="btn btn-warning">Delete</button>
                                </form>
                                        <a href="<%="/rooms/"+rooms[i]._id%>/edit">
                                        <button type="button" class="btn btn-primary">Edit</button></a>
                                        
                            </div>

                                            
                                    </div>
                                </div>
                                <% } %>
                                <% } else { %>
                                    <h3> No Listings Found</h3>
                                    <% } %> 
                                
                            </div>
                        </div>
                    </div>
                </div>
                        
                        <div class="mt-5" style="background: #FF664D;height: 50px;"><span>&nbsp;</span></div>

</body>

</html>